apiVersion: v1
kind: Service
metadata:
  name: {{ include "microservice-helmchart.fullname" . }}
  labels:
    {{- include "microservice-helmchart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "microservice-helmchart.selectorLabels" . | nindent 4 }}
