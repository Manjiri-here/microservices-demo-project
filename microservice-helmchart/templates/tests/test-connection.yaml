apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "microservice-helmchart.fullname" . }}-test-connection"
  labels:
    {{- include "microservice-helmchart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "microservice-helmchart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
